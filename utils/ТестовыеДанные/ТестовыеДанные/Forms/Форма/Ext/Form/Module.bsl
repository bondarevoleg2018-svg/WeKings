
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Маркер = Новый ЗаписьТекста("C:\Bases\WeKings_marker.txt", КодировкаТекста.UTF8);
	Маркер.ЗаписатьСтроку("форма создана");
	Маркер.Закрыть();
	
	Попытка
		ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
		ОбработкаОбъект.ВыполнитьВсеТесты();
	Исключение
		// Ошибки фиксируются в файле лога внутри ВыполнитьВсеТесты
	КонецПопытки;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ЗавершитьРаботуСистемы(Ложь);
	
КонецПроцедуры

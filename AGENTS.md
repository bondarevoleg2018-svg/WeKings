# Проект WeKings (учебная конфигурация 1С)

Восстановленная с нуля конфигурация учёта performance digital-агентства
(iGaming): клиенты — казино/беттинг, веб-мастера — источники трафика.

## Среда
- Платформа (учебная): `C:\Program Files (x86)\1cv8t\8.3.27.1508\bin\1cv8t.exe`
- База: файловая `C:\Bases\InfoBase2`
- Исходники: эта папка — файлы выгрузки Конфигуратора (Hierarchical, формат 2.20)
- Синтаксис: русский, управляемые формы

## Режим auto (жёсткие правила)
- Удаление запрещено. Вместо удаления — новая версия файла/папки (dump_v1, dump_v2).
- После каждой итерации: `git add -A && git commit -m "Итерация N: <что сделано>"`.
  Без коммита итерация не считается законченной.
- Пакетная загрузка: `load.cmd` (1cv8t.exe DESIGNER /LoadConfigFromFiles /UpdateDBCfg /Out log.txt).
  После каждого запуска читать log.txt целиком; пока есть ошибки — чинить и перезапускать.
- Отклоняться от утверждённой модели метаданных запрещено. Изменения модели —
  только как «предлагаю изменение: …» в отчёте, работа продолжается по старой модели.
- Отчёт после итерации: созданные объекты, статус log.txt, номер коммита,
  предлагаемые изменения. 3–5 строк.

## Утверждённая модель (кратко)
- Перечисления: МоделиРасчёта (CPA/RevShare/Гибрид), ТипыКонверсий (Лид/Депозит),
  СтатусыАкта (Черновик/Выставлен/Оплачен).
- Справочники: Валюты, Страны, Клиенты, КаналыТрафика (предопределённые
  PPC/In-App/Social/SEO/Affiliate), ВебМастера, Офферы (подчинены Клиентам),
  РекламныеКампании.
- Константа УправленческаяВалюта (СправочникСсылка.Валюты).
- РС: КурсыВалют (периодический), УсловияВебМастеров (периодический;
  ВебМастер+Оффер; Ставка, МодельВыплаты).
- РН: Конверсии (обороты; Кампания; Лиды, Депозиты, СуммаДепозитов),
  ВзаиморасчётыСКлиентами (остатки; Клиент+Валюта; Сумма, СуммаУпр),
  ВзаиморасчётыСВебМастерами (остатки; ВебМастер+Валюта; Сумма, СуммаУпр),
  НачисленияВебМастерам (обороты; ВебМастер, Кампания, Оффер; СуммаУпр),
  РекламныеРасходы (обороты; Кампания, Канал; СуммаУпр).
- Документы: ЗагрузкаЛидов, АктКлиенту, ОплатаОтКлиента, НачислениеВебМастерам,
  ВыплатаВебМастеру, РасходНаРекламу.
- Роли: Медиабаер, AffiliateМенеджер, Бухгалтер, Руководитель.
- 5 отчётов СКД: Конверсии по кампаниям, Взаиморасчёты с клиентами,
  Расчёты с веб-мастерами, Выручка по моделям расчёта, ROI по кампаниям.

## Стандарты кода
- Без запросов в цикле; параметры виртуальных таблиц; ВЫРАЗИТЬ для составных типов;
  транзакции через Попытка/Исключение с ОтменитьТранзакцию и пробросом исключения.

## Известные остатки
- В базе InfoBase2 присутствуют объекты демо «ДемоСклад» (Товары, ПриходТоваров,
  СкладскойУчет) из прошлых опытов. Они не входят в проект и не мешают загрузке.
